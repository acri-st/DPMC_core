apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: eocp

resources:
  - secret.yml
  - service.yml
  - statefulset.yml

labels:
  - includeSelectors: true
    pairs:
      app: keycloak-db

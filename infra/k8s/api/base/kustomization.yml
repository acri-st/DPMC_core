apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: eocp

resources:
  - secret.yml
  - configmap.yml
  - deployment.yml
  - service.yml
  - ingress.yml

labels:
  - includeSelectors: true
    pairs:
      app: api

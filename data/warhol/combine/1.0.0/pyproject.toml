[project]
name = "warhol-combine"
version = "1.0.0"
description = "PC6 — combine four tinted images into a 2x2 grid."
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["ruff>=0.9"]

[tool.uv]
package = false

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "UP", "SIM"]
